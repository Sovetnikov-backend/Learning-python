"""
ВЕРСИЯ 1.2: Детализированная система контроля производства
Добавлены вложенные условия для уточнения ошибок
"""

print("=" * 50)
print("СИСТЕМА КОНТРОЛЯ ПРОИЗВОДСТВЕННЫХ ПАРАМЕТРОВ")
print("Версия 1.2: С детализированными сообщениями об ошибках")
print("=" * 50)

# Ввод данных
temperature = int(input("Температура (°C): "))
pressure = int(input("Давление (бар): "))
raw_material = int(input("Сырьё (кг): "))

print("\n" + "=" * 50)
print("РЕЗУЛЬТАТЫ ПРОВЕРКИ")
print("=" * 50)

# Счётчик проблем
problems_count = 0

# 1. ПРОВЕРКА ТЕМПЕРАТУРЫ С ДЕТАЛИЗАЦИЕЙ
if 18 <= temperature <= 25:
    print(f"✅ Температура: {temperature}°C (норма: 18-25°C)")
else:
    problems_count += 1
    print(f"❌ Температура: {temperature}°C")

   # Вложенное условие для детализации
    if temperature < 18:
        difference = 18 - temperature
        print(f"   ⚠️  Слишком низкая! Нужно нагреть на {difference}°C")
    else: # значит temperature > 25
        difference = temperature - 25
        print(f"   ⚠️  Слишком высокая! Нужно охладить на {difference}°C")

# 2. ПРОВЕРКА ДАВЛЕНИЯ С ДЕТАЛИЗАЦИЕЙ
if 100 <= pressure <= 200:
    print(f"✅ Давление: {pressure} бар (норма: 100-200 бар)")
else:
    problems_count += 1
    print(f"❌ Давление: {pressure} бар")

    # Вложенное условие
    if pressure < 100:
        difference = 100 - pressure
        print(f"   ⚠️  Слишком низкое! Нужно повысить на {difference} бар")
    else: # pressure > 200
        difference = pressure - 200
        print(f"   ⚠️  Слишком высокое! Нужно снизить на {difference} бар")  

# 3. ПРОВЕРКА СЫРЬЯ
if raw_material > 500:
    print(f"✅ Сырьё: {raw_material} кг (достаточно, нужно > 500кг)")
else:
    problems_count += 1
    print(f"❌ Сырьё: {raw_material} кг")
    difference = 500 - raw_material
    print(f"   ⚠️  Недостаточно! Нужно добавить {difference} кг")

print("\n" + "=" * 50)
print("ИТОГОВОЕ РЕШЕНИЕ")
print("=" * 50)

# Проверяем все условия с помощью AND (изученная тема)
all_conditions_ok = (18 <= temperature <= 25 and 100 <= pressure <= 200 and raw_material > 500)

if all_conditions_ok:
    print("🎉 ВСЕ ПАРАМЕТРЫ В НОРМЕ!")
    print("СМЕНА МОЖЕТ НАЧИНАТЬСЯ ✅")
else:
    print("🚫 ЕСТЬ НАРУШЕНИЯ ПАРАМЕТРОВ")
    print("СМЕНУ НАЧИНАТЬ НЕЛЬЗЯ ❌")

 # Используем вложенные условия для детализации итога
if problems_count == 1:
    print(f"\nОбнаружена {problems_count} проблема.")
    print("Необходимо устранить её перед началом смены.")
elif problems_count == 2:
    print(f"\nОбнаружены {problems_count} проблемы.")
    print("Необходимо устранить обе проблемы.")
elif problems_count == 0:
    print(f"\nОбнаружено {problems_count} проблем.")
else: # problems_count == 3
    print(f"\nОбнаружены {problems_count} проблемы.")
    print("Необходимо устранить все проблемы.")

print("=" * 50)
